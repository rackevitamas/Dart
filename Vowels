void main() {
  String myString = "apple";
  int vowelCount = 0;
  String vowels = "aeiouAEIOU";

  for (int i = 0; i < myString.length; i++) {
    if (vowels.contains(myString[i])) {
      vowelCount++;
    }
  }

  print('The number of vowels in the word "$myString" is $vowelCount.');
}
